export interface Task {
  _id: string;
  title: string;
  requirements: string;
  notes: string;
  customerId: string;
  dueDate: string;
  status: string;
  userId: string;
  createdAt: string;
  updatedAt: string;
}
